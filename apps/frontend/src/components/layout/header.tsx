/**
 * header.tsx — DEPRECATO
 *
 * Questo componente non è più usato.
 * Il layout tenant incorpora ora l'header direttamente in:
 *   apps/frontend/src/app/(tenant)/layout.tsx
 *
 * Mantenuto per evitare errori su eventuali import residui.
 */
export {};
